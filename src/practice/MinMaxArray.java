/**
* Given an array A of n integers and and integer X. You may choose any integer 
* between -X <= k <=X, and add k to A[i] for each 0 <= i <= n-1. 
* The task is to find the smallest possible difference between the maximum 
* value of A and the minimum value of A after updating the array A.
*/
package practice;

import java.util.Scanner;

public class MinMaxArray {

    public static void main(String[] args) {
        int[] intArray;
        Scanner in = new Scanner(System.in);
        System.out.print("Enter array length: ");
        int arrayLength = in.nextInt();
//        int[] intArray = new int(arrayLength);
        for (int j=0 ; j < arrayLength; j++) {
            
        }
        
    }
    
}
